// Поверхня з тонкою рамкою (без великих карток і тіней).
import QtQuick
import Gmdr.Ui

Rectangle {
    color: Theme.surface
    border.color: Theme.border
    border.width: 1
    radius: Theme.r2
}
