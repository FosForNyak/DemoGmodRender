// =============================================================================
//  crash_dump.hpp — звіт про аварійне завершення програми.
//
//  Якщо програма впаде (помилка доступу до пам'яті тощо), у папці програми
//  з'явиться gmdr_crash_<дата>.dmp — знімок пам'яті, за яким розробник може
//  знайти місце збою у Visual Studio. Разом із gmdr_log.txt його варто
//  додати до повідомлення про помилку.
// =============================================================================
#pragma once

#include <filesystem>

namespace gmdr {

// Встановити обробник (Windows). dir — куди писати файли .dmp.
void install_crash_handler(const std::filesystem::path& dir);

} // namespace gmdr
